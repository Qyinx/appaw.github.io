/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  trailingSlash: true,
  images: {
    unoptimized: true,
  },
  // Custom domain (appaw.store) - no basePath needed
}

module.exports = nextConfig
